/**
 * The staff code the test server accepts. It exists only for the suite:
 * playwright.config.ts hands it to the server in place of the real STAFF_KEY.
 */
export const E2E_STAFF_KEY = 'e2e-staff-key'
